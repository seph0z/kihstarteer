@using System.Collections.Generic
@model List<string>

@foreach (var img in Model)
{
    <img src="@img" id="imgs" class="mx-auto d-block" alt="Alternate Text" />
    <script>
        document.getElementById('uploadImage').value = "@img";
    </script>
}